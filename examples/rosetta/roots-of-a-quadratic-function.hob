// @test: [quadratic((1.0, -3.0, 2.0)), quadratic((1.0, 0.0, -4.0)), quadratic((1.0, -2.0, 1.0))]
quadratic x = match x with
  | (a, b, c) -> let disc = b * b - 4.0 * a * c in let sd = sqrt(disc) in ((-b + sd) / (2.0 * a), (-b - sd) / (2.0 * a))
